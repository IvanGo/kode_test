# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'kode_test' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for kode_test

  pod 'Alamofire', '~> 4'
  pod 'SwiftyJSON'
  pod 'SnapKit'

  target 'kode_testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'kode_testUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
